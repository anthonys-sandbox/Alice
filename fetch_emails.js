import { google } from 'googleapis';
import { authenticate } from '@google/local-auth';
import fs from 'fs';

// If we can't do this easily, I can use the gws workspace_search or gws workspace_pipeline tool to get the plain text!
